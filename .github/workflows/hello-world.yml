name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "Hello World!"
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: myartifact
          path: /c/Users/bhagyaraj.j/Downloads/demo/azureDevOps/dist/*.txt
          if-no-files-found: error
